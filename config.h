#ifndef CONFIG_H
#define CONFIG_H

#define HEIGHT 480
#define WIDTH 320
#define GRID_SIZE 20
#define COLS (HEIGHT / GRID_SIZE)
#define ROWS (WIDTH / GRID_SIZE)
#define MAX_PART (ROWS * COLUMNS)

#endif